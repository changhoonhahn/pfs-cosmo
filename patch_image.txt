#making list of ra,dec
d = {'ra':ra[targets], 
     'dec': dec[targets]}
df = pd.DataFrame(data=d)
np.savetxt("filename.txt",df,header="ra,dec")
#you need to change the header from # to #?

#showing images
with tarfile.open('filename.tar','r') as t:
    t.extractall(path='directory_name')

import os
filedir = './directory_name/**'
files = glob.glob(filedir+'/*')
filenames = []
for file in files:
    filename_1 = os.path.splitext(os.path.basename(file))[0]
    filenames.append('.\\directory_name\\filename\\'+filename_1)
print(filenames)

fig=plt.figure(figsize=(50,50))
for i in range (number of images):
    filename=filenames[i]+'.fits'
    data1=fits.getdata(filename,1)
    ax=fig.add_subplot(5,6,i+1)
    ax.imshow(data1,cmap='gray')